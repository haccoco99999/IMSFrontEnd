import {GET_ALL_CATEGORY_REQUEST,GET_ALL_CATEGORY_RESPONSE,GET_ALL_CATEGORY_ERROR}
    from '../../constants'

    const initalState = {
        requesting: false,
        successful: false,
        messages: "",
        errors: "",
        listAccounts:[]
    }

    export default function reducer(state=initialState,action){
        
    }